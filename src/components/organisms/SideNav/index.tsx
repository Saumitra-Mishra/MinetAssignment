import { Grid } from "@mui/material"



const SideNav = ({data}: any) => {
    return <Grid>
        {/* Map the icons in the Grid and add on Click Functionality for Each of them */}
    </Grid>
}

export default SideNav;