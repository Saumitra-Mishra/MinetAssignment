import { Box } from "@mui/material"
import { useEffect } from "react";


const OrderList = ({data} : any) => {
    useEffect(() => {
        //api call for adding elemeent to List
        //api call for deleting element from the list
    },data)
    return <Box>
        {/* map the data */}
    </Box>
}

export default OrderList;